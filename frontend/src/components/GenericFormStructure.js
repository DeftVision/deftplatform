import {Box, Checkbox, TextField, Typography} from '@mui/material';

import React from 'react';

const GenericFormStructure = ({form_fields}) => {
    return (
        <div>
           {/* {form_fields.map((form_field => (
                <TextField/>
                <TextField />
                <Checkbox />            ))}


*/}
        </div>
    );
};

export default GenericFormStructure;
